# 影响面检查报告

状态：`review_required`

## 已确认

- 目标目录：`/Users/fan/Documents/底 IG 大米外贸`。
- 当前目录不是 Git 仓库，没有可确认的 Git 根目录、分支或目标仓库未提交状态。
- 本轮输出目录此前不存在，已使用首个版本目录 `project_bootstrap/collaboration_mechanism_migration/`；没有覆盖同名目录。
- 当前项目根目录未发现正式 `AGENTS.md`、`README.md`、ChatGPT Project 同步包或既有同类迁移目录。
- 当前项目已有 `foreign_trade_low_gi_plan/`，包含 DOCX/CSV 业务方案、国家排序、B2B/B2C 方案、验收标准、风险清单和待确认问题。
- 主参考仓库本地只读副本 HEAD 与远端 `main` 一致：`ebc420f7bf42bd8c6c1d9ed435fd889177aa452c`。
- 主参考仓库在本轮前已有未跟踪 `public/`；该目录不是本轮产生，本轮不读取、不修改、不归因。

## 发现的冲突

附件任务说明明确规定：当前尚未把任何市场、国家、平台、商业模式或执行方案认定为正式结论。既有 `foreign_trade_low_gi_plan/` 中却出现“主结论”“优先国家”“B2B 为主”等确定性表述，同时也声明整体仍是“前期判断 + 前期验证”并保留大量 `待确认`、`阻断项`。

因此本轮作如下裁决：

- 既有资料是重要项目上下文，但其正式权威等级无法仅凭文件存在确认。
- 不把其中的国家、渠道、平台、比例、话术或 30 天计划迁入“当前正式事实”。
- 不删除、不修改、不重命名这些资料。
- 在候选规则中要求后续由用户逐项确认：哪些是正式决策，哪些是研究建议，哪些已经失效。
- 在完成此人工裁决前，新项目动态事实层保持空模板，整体状态维持 `review_required`。

## 既有机制检查

| 检查项 | 结果 | 处理 |
|---|---|---|
| `AGENTS.md` | 未发现 | 只生成候选版，不激活 |
| `README.md` | 未发现 | 不新建或覆盖正式 README |
| 正式事实文件 | 未发现明确单一权威文件 | 只生成空模板 |
| 项目说明/业务方案 | 已存在 DOCX/CSV 资料包 | 只读；记录权威冲突 |
| 项目日志 | 未发现明确 latest/history 日志体系 | 候选架构中提出分层建议 |
| ChatGPT Project 同步包 | 未发现 | 生成本轮静态候选上传清单 |
| 类似协作迁移目录 | 未发现 | 使用规定新目录 |

## 本轮写入影响

- 仅新增 `project_bootstrap/collaboration_mechanism_migration/` 内文件。
- 不修改 `.omx/`、`foreign_trade_low_gi_plan/` 或根目录其他文件。
- 不执行外部业务动作，不改变任何业务状态。
- 目标目录不是 Git 仓库，故 `git diff` 检查记为 `not_applicable`；使用任务前后文件清单和允许前缀检查替代。

## 合并建议

1. 用户先审核 `06_当前正式事实模板.md`，逐项确认既有 DOCX 中哪些结论可升级为正式事实。
2. 再审核 ChatGPT Project 指令候选版与 AGENTS 候选版的职责边界。
3. 最后选择是否把候选文件复制到正式位置；本轮不自动合并或激活。

## Tested

- `pwd` 与目录存在性检查。
- Git 根目录、分支、状态探测。
- 根目录关键文件与同类迁移目录扫描。
- 既有 DOCX 关键语义只读抽取。
- 主仓库本地 HEAD 与远端 `main` SHA 对读。

## Not-tested

- 未对既有 DOCX 中的市场研究结论进行业务真实性复核。
- 未确认既有 DOCX 是否经过用户逐项正式批准。
- 因目标目录不是 Git 仓库，无法运行目标仓库 `git diff`。
